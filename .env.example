# Copy to .env and fill in for local development.
# Postgres connection string (the platform injects this in preview and in production).
DATABASE_URL=postgres://user:password@localhost:5432/toolnest

# Public origin of the site. Used for canonical URLs, hreflang, sitemap.xml and robots.txt.
MYTHEX_WEB_ORIGIN=http://localhost:3000
